import ParallaxLanding from '@/components/ParallaxLanding';

export const metadata = {
  title: 'Layover Friends - Connect During Airport Layovers',
  description: 'Meet fellow travelers during airport layovers. Real-time chat, flight tracking, AI travel assistant - all in one app.',
};

export default function Home() {
  return <ParallaxLanding />;
}
